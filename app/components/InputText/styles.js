import {
  StyleSheet
} from 'react-native'

export default StyleSheet.create({
  textInput: {
    fontFamily: 'futura',
    alignSelf: 'center',
    height: 60,
    width: 300,
    backgroundColor: 'white',
    borderRadius: 10,
    padding: 20
  }
})
